/** Public genesis-env repository (source of truth for nav / CTAs / footer links). */
export const GITHUB_REPO_URL = 'https://github.com/Alen983/genesis-env' as const

export const GITHUB_ISSUES_URL = `${GITHUB_REPO_URL}/issues` as const
